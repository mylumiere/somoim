import { SignUpValidatorDirective } from './sign-up-validator.directive';

describe('SignUpValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SignUpValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
